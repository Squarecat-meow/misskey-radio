import React from "react";
import ModalWindow from "../components/ModalWindow";

const serverTokenInput = () => {
  return (
    <div>
      <ModalWindow />
    </div>
  );
};

export default serverTokenInput;
